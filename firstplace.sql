
CREATE TABLE `firstplace` (
  `id` int NOT NULL,
  `horse_name` varchar(45) NOT NULL,
  `year_raced` int NOT NULL,
  `earnings` int NOT NULL,
  `winning_length` double NOT NULL,
  `winning_time` double NOT NULL,
  PRIMARY KEY (`id`));
